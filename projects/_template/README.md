# Project template

Copy this directory to `projects/<slug>/`. Keep implementation, experiments, artifacts, receipts, notebooks, and local project memory together.

Registered experiments use `experiments/<EXP-ID>-<slug>/` and are developed on an experiment branch until human merge.
