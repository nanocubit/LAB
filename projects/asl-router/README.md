# ASL-Router v0.1

First resident LAB project. It tests whether deterministic, scoped, replayable, reversible adaptive state can improve retrieval strategy selection relative to a frozen baseline without bypassing evidence, authority, cost, latency, or risk constraints.

See `docs/ASL_CONTRACT_v0.1.md` and `experiments/EXP-ASL-0001-synthetic-baseline/`.
