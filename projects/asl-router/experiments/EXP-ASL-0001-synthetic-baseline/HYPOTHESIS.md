# H-ASL-0001

A frozen `vector_only` retrieval strategy is optimal in one stable synthetic phase but incurs measurable regret after controlled distribution drift. This baseline experiment establishes the oracle comparison surface before introducing an adaptive PolicyProjection.
